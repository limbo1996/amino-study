# AGENTS.md

- Repo is currently empty except for `init/`; no build/test/lint/tooling files found.
- Workflow rule for every change: call `brainstorming` first, then `planning-with-files-zh`, then use `test-driven-development` for verification, and finish with `ralph` confirmation.
- Each change must be committed with a clear git commit message.
- Add or update this file once real packages, scripts, or CI config exist.
